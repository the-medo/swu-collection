# Plan: Preview Card Karabast ID Mapping

## Goal

Add a new optional preview-card payload property, `karabast_id_to_swubase_id`, and use it when importing Karabast game results so `game_result.leader_card_id`, `game_result.base_card_key`, `game_result.opponent_leader_card_id`, and `game_result.opponent_base_card_key` store SWUBase card IDs whenever a preview-card mapping exists.

This field is different from the existing `karabast_id`:

- `karabast_id` is used when SWUBase exports preview cards to Karabast.
- `karabast_id_to_swubase_id` will be used when SWUBase receives Karabast IDs and needs to resolve them back to SWUBase card IDs.

## Current Findings

- Preview cards are JSON payloads stored in `preview_card.payload`.
- The admin creation template is generated by `server/lib/cards/previewCardPayload.ts`.
- The frontend admin API type is duplicated in `frontend/src/api/admin/previewCards.ts`.
- The shared card-list type is in `lib/swu-resources/types.ts`.
- Preview-card documentation is in `docs/preview-cards/preview-card-docs.md`.
- Karabast game result imports currently resolve card IDs through `shared/lib/cardUidToCardId.ts`.
- `cardUidToCardId()` checks official `cardsByUid` from `server/db/lists.ts`, applies the special base key mapping when requested, and falls back to the original UID.
- `server/lib/game-results/transformKarabastGameDataToGameResults.ts` uses that resolver for the four `game_result` leader/base columns.
- `server/lib/game-results/resolveKarabastLobbyMatchIds.ts` also uses the same resolver for opponent leader/base identity lookup keys. This is not the final `game_result` write, but should probably be kept consistent so preview-card IDs do not fragment Karabast match resolution.

## Product Decisions

- `cardMetrics` keys should also resolve through `karabast_id_to_swubase_id`, not only the four leader/base columns.
- Duplicate active `karabast_id_to_swubase_id` values should warn for now, not fail validation or block preview-card saves.
- Only active preview cards should participate in inbound Karabast mapping right now.

## Implementation Plan

1. Add the optional preview-card field everywhere payload shape is declared.

   Update:

   - `lib/swu-resources/types.ts`
   - `server/lib/cards/previewCardPayload.ts`
   - `frontend/src/api/admin/previewCards.ts`

   Details:

   - Add `karabast_id_to_swubase_id?: string`.
   - Keep the snake_case naming, matching the existing `karabast_id` convention.
   - Add it to the Zod payload schema as optional.
   - Add it to `PreviewCardPayload` types.
   - Add it to `createPreviewCardPayloadTemplate()` as an empty string so admins see it in the JSON textarea when creating new preview cards, even though the field remains optional.
   - Preserve `.passthrough()` behavior so existing saved payloads remain valid.

2. Decide exact matching semantics for `karabast_id_to_swubase_id`.

   Planned interpretation:

   - The value stores the inbound Karabast ID/UID/internalName that Karabast sends for this card.
   - When an incoming Karabast card UID matches a card payload's `karabast_id_to_swubase_id`, the resolver returns that payload's SWUBase `cardId`.
   - Trim both the incoming UID and the preview-card field before matching.
   - Ignore empty strings.
   - If duplicate preview-card mappings exist, prefer a deterministic behavior and surface enough logging to fix the data. Recommended behavior: first active merged-list entry wins according to object iteration, but log a warning for duplicates.

3. Add a preview-aware Karabast card resolver.

   Recommended shape:

   - Keep `shared/lib/cardUidToCardId.ts` as the official-list resolver, because it is synchronous and currently used in shared-ish code.
   - Add a server-side helper near the Karabast import flow, for example `server/lib/game-results/resolveKarabastCardId.ts`.
   - The helper should:
     - First call the existing `cardUidToCardId(uid, useSpecialBaseKey)`.
     - If the official resolver found a real official card ID, return it.
     - If the official resolver only fell back to the original UID, then check active preview/merged cards for `karabast_id_to_swubase_id`.
     - If a mapping is found, return the matching preview card's `cardId`.
     - If `useSpecialBaseKey` is true, apply `baseSpecialNames` to the resolved preview card ID as well, matching current base behavior.
     - If no mapping is found, keep the existing fallback behavior and return the original UID.

   Important distinction:

   - Because `cardUidToCardId()` currently returns the original UID as a fallback, the new helper needs to detect whether the official lookup actually matched.
   - The cleanest approach is to add or expose a lower-level official lookup that can return `{ cardId, matchedOfficial }`, or to check `cardsByUid[uid]` directly in the new server helper.

4. Build the preview mapping from active preview cards.

   Options:

   - Use `getMergedCardList()` so official and preview cards are both visible, with official cards winning on `cardId` collisions.
   - Or use `getPreviewCardList()` to make it explicit that this fallback is preview-only.

   Recommended:

   - Use `getPreviewCardList()` for this specific fallback because the field is meant for preview-card metadata.
   - Build a `Map<string, string>` from trimmed `karabast_id_to_swubase_id` to `card.cardId`.
   - Build it once per transformation/resolution run instead of repeatedly per field.
   - Consider caching inside the helper only if needed; the preview card provider already has an in-memory cache, so a small map build per import is acceptable.

5. Update Karabast game-result transformation.

   Update `server/lib/game-results/transformKarabastGameDataToGameResults.ts`:

   - Load/create the preview-aware resolver before looping players.
   - Replace:
     - `leaderCardId: cardUidToCardId(player.data?.leader)`
     - `baseCardKey: cardUidToCardId(player.data?.base, true)`
     - `opponentLeaderCardId: cardUidToCardId(opponent?.data?.leader)`
     - `opponentBaseCardKey: cardUidToCardId(opponent?.data?.base, true)`
   - With the preview-aware resolver.
   - Update `mapCardMetricsKeysToCardIds()` to use the same preview-aware resolver so preview-card metrics aggregate under SWUBase IDs instead of staying under raw Karabast IDs.
   - Keep the existing behavior of dropping metric entries whose incoming key cannot produce any usable card ID.

6. Keep Karabast match identity resolution consistent.

   Update `server/lib/game-results/resolveKarabastLobbyMatchIds.ts` to use the same preview-aware resolver for:

   - `opponentLeaderCardId`
   - `opponentBaseCardKey`

   This makes lobby-match lookup keys consistent with the final `game_result` rows. Because `getKarabastLobbyMatchIdentities()` is currently synchronous, this will require either:

   - Making it async and adjusting `resolveKarabastLobbyMatchIds()`, or
   - Passing in a prebuilt synchronous resolver function.

   Recommended:

   - Add a helper that asynchronously creates a synchronous resolver function, for example `createKarabastCardIdResolver()`.
   - Use that resolver in both game-result transformation and lobby-match identity construction.

7. Update tests.

   Existing tests live in `server/lib/cards/previewCardPayload.test.ts`; there do not appear to be current `server/lib/game-results/*.test.ts` files.

   Add focused tests for:

   - `normalizePreviewCardPayload()` preserves `karabast_id_to_swubase_id`.
   - `createPreviewCardPayloadTemplate()` includes `karabast_id_to_swubase_id: ''`.
   - The new Karabast resolver returns official mappings before preview mappings.
   - The new Karabast resolver maps an unknown Karabast UID to a preview card's SWUBase `cardId` when `karabast_id_to_swubase_id` matches.
   - Base resolution still applies `baseSpecialNames` when `useSpecialBaseKey` is true.
   - Fallback behavior remains unchanged for unknown IDs without a preview mapping.
   - `cardMetrics` keys are resolved through the same preview-aware mapping.
   - Duplicate preview mappings emit a warning but do not throw.

   Depending on how hard DB-backed preview-card cache setup is in tests, prefer extracting a pure function like `buildKarabastPreviewIdMap(cardList)` and `resolveKarabastCardIdWithMap(...)` so the resolver behavior can be tested without a database.

8. Update documentation.

   Update `docs/preview-cards/preview-card-docs.md`:

   - Add `karabast_id_to_swubase_id?: string` to the payload extension list.
   - Explain the difference from `karabast_id`.
   - Add an admin checklist note for filling it when Karabast sends a temporary preview ID that does not resolve via official `cardUid`.
   - Add a short note in the Karabast section that inbound game-result imports resolve official IDs first, then preview `karabast_id_to_swubase_id`, then fall back to the received UID.

## Verification Plan

Run focused checks after implementation:

1. `bun test server/lib/cards/previewCardPayload.test.ts`
2. Any new resolver test file, for example `bun test server/lib/game-results/resolveKarabastCardId.test.ts`
3. Typecheck if the repo has a configured command available; otherwise run a targeted import/build smoke check with Bun.
4. Manually verify the admin preview-card JSON template returned by `GET /api/admin/preview-cards` includes both `karabast_id` and `karabast_id_to_swubase_id`.

## Open Questions

- None currently. The previous open questions are captured under Product Decisions.
